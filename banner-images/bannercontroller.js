(function () {
    'use strict';

    angular
        .module('MyApp')
        .controller('bannerController', bannerController);

    bannerController.$inject = ['$location', '$http', '$state', '$window','$scope' , '$document'];
    function bannerController($location, $http, $state ,  $window, $scope, $document) {
        
        
  

}
})();